# This file tells Git how to store certain files.

# Automatically detect text files and store them using LF line endings.
* text=auto eol=lf

# Store images using Git LFS.
*.jpg filter=lfs diff=lfs merge=lfs -text
*.jpeg filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text
